# Third-Party Notices

`ev_cost_recovery` binaries bundle the crates listed below. Each section gives a
license, the crates distributed under it, and its full text. The project's own
license is stated in `LICENSE-MIT` and `LICENSE-APACHE`.

Generated by `scripts/gen-notices.sh`. Do not edit by hand: release builds verify
the stamp at the end of this file against the dependency graph, and an edited
copy will fail to build.

## Licenses in use

{{#each overview}}
- {{{name}}} ({{count}})
{{/each}}

{{#each licenses}}
## {{{name}}}

Used by:

{{#each used_by}}
- {{crate.name}} {{crate.version}}
{{/each}}

```
{{{text}}}
```

{{/each}}
